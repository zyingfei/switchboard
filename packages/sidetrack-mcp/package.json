{
  "name": "@sidetrack/mcp",
  "description": "Read-only MCP server for Sidetrack vault state.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "bin": {
    "sidetrack-mcp": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "build": "bunx --bun --no-install tsc -p tsconfig.build.json",
    "typecheck": "bunx --bun --no-install tsc --noEmit -p tsconfig.json",
    "lint": "bunx --bun --no-install eslint \"**/*.{ts,tsx}\"",
    "format": "bunx --bun --no-install prettier --write .",
    "format:check": "bunx --bun --no-install prettier --check .",
    "test": "bunx --no-install vitest run",
    "test:watch": "bunx --no-install vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "minisearch": "^7.2.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^24.10.1",
    "eslint": "^9.39.1",
    "prettier": "^3.7.4",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.4",
    "vitest": "4.1.5"
  }
}
