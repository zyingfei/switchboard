{
  "name": "bac-local-bridge-companion-poc",
  "description": "Local writer companion PoC for Browser AI Companion vault writes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "compile": "bunx --bun --no-install tsc --noEmit",
    "start": "bun src/cli.ts",
    "test": "bunx --no-install vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
