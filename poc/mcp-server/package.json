{
  "name": "browser-ai-companion-mcp-server-poc",
  "description": "Standalone MCP server POC for Browser AI Companion",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsup src/cli.ts src/smoke.ts --format esm --platform node --target node22 --out-dir dist --sourcemap",
    "compile": "tsc --noEmit",
    "start": "tsx src/cli.ts",
    "smoke": "tsx src/smoke.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.8.1",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "tsup": "^8.5.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^2.1.8"
  }
}
